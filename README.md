# FlyRank Backend Track - Task API

A lightweight CRUD API built with Python and FastAPI that manages an in-memory to-do list.

## Setup & Running

1. Activate your virtual environment and install requirements:
   ```bash
   pip install fastapi uvicorn pydantic